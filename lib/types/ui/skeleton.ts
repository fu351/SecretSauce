/**
 * Skeleton Line Props Type
 *
 * Props for skeleton loading line components.
 * Used to create placeholder UI while loading recipe and other data.
 */
export interface SkeletonLineProps {
  className?: string
}

/**
 * Skeleton Component Props Type
 *
 * Base props for skeleton loading components.
 * Can be extended for specific skeleton component needs.
 */
export interface SkeletonComponentProps {
  // Common props for skeleton components if needed
}
