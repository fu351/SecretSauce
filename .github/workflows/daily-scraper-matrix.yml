name: Daily Scraper Matrix

on:
  workflow_call:
  workflow_dispatch:

jobs:
  scrape-stores:
    name: Scrape ${{ matrix.store }}
    runs-on: ubuntu-latest
    timeout-minutes: 45

    strategy:
      fail-fast: false
      matrix:
        store: [walmart, target, safeway, kroger, aldi, traderjoes, wholefoods, andronicos, meijer, "99ranch"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Scrape ${{ matrix.store }}
        env:
          STORE_BRAND: ${{ matrix.store }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Starting scraper for ${{ matrix.store }}"
          node scripts/daily-scraper.js

  optimize-database:
    name: Optimize Database
    needs: scrape-stores
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: always()

    steps:
      - name: Run ANALYZE
        env:
          DB_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "📈 Updating database statistics..."
          psql "$DB_URL" -c "ANALYZE public.ingredients_history;"
          psql "$DB_URL" -c "ANALYZE public.ingredients_recent;"

      - name: Verify results
        env:
          DB_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "📊 Checking results..."
          COUNT=$(psql "$DB_URL" -t -A -c "SELECT COUNT(*) FROM public.ingredients_recent;")
          echo "Total rows in ingredients_recent: $COUNT"

          if [ "$COUNT" -lt 100 ]; then
            echo "⚠️ WARNING: Very few rows inserted ($COUNT)"
            exit 1
          fi

          echo "✅ Scraping complete with $COUNT total results"
